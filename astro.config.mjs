// @ts-check
import { defineConfig } from 'astro/config';

// Static output, framework-free. No `site` yet: URLs must not be fabricated
// before a real hosting origin exists (portfolio-content spec).
export default defineConfig({});
