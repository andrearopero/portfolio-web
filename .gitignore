# CV contains prohibited personal data (phone + references) — never enters git history
HDV_AndreaRopero.pdf
node_modules/
dist/
.astro/

# TypeScript incremental build cache
*.tsbuildinfo

# Local workspace scaffolding — project planning artifacts, not site source
.atl/
openspec/
